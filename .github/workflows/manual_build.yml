name: Manual build

on:
  workflow_dispatch:
    inputs:
      os:
        description: 'OS (ubuntu, windows, macos)'
        required: true
      target:
        description: 'Make target'
        required: false
        default: 'test'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ${{ github.event.inputs.os }}-latest
        ocaml-version:
          - 4.08.1
        include:
          - os: windows-latest
            make_params: OSARCH=win32

    runs-on: ${{ matrix.os }}

    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2

    - name: Cache opam
      id: cache-opam
      uses: actions/cache@v2
      if: runner.os != 'Windows'
      with:
        path: ~/.opam
        key: ${{ matrix.os }}-ocaml_${{ matrix.ocaml-version }}-opam

    - name: Set up opam
      if: steps.cache-opam.outputs.cache-hit == 'true' && runner.os == 'Ubuntu'
      run: sudo apt-get -y install bubblewrap ocaml-native-compilers ocaml-compiler-libs musl-tools

    - name: Set up opam
      if: steps.cache-opam.outputs.cache-hit == 'true' && runner.os == 'macOS'
      run: /usr/local/bin/brew install opam

    - name: Use OCaml ${{ matrix.ocaml-version }}
      if: steps.cache-opam.outputs.cache-hit != 'true'
      uses: avsm/setup-ocaml@v1
      with:
        ocaml-version: ${{ matrix.ocaml-version }}

    - run: opam exec -- make ${{ matrix.make_params }}

    - run: make ${{ github.event.inputs.target }}
